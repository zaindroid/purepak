# node
node_modules
package-lock.json

# local runtime data (never ship databases/receipts to the repo)
server/data/
server/*.log

# Android build output
android/.gradle
android/build
android/app/build
android/local.properties
android/app/google-services.json
keystore/

# test artifacts
webtest/shots
webtest/phone-audit
*.apk

# local-only secrets reference (never commit)
.env.local

# OS
.DS_Store
Thumbs.db
